name: CD

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

  workflow_dispatch:

jobs:
  build:
    runs-on: macOS-latest

    steps:
      - name: Deploy to SPM
        run: swift package generate-xcodeproj
